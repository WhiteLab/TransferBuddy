# TransferBuddy
REST Sequence Data Transfer

```
pip install eve
pip install requests
```

To start producer:

```$ python tbuddy.py --producer```

To run a subscriber:

```$ python tbuddy.py --subscriber```
